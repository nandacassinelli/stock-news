import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    height: 240,
    backgroundColor: '#eee',
    borderRadius: 24,
    marginHorizontal: 16,
    marginTop: 16
  },
  text: {
    fontSize: 18,
    fontWeight: '600',
    lineHeight: 24,
    paddingBottom: 24
  }
})

export default styles
